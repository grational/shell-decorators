package it.italiaonline.rnd.shell

/**
 * A class implementing this interface ideally have to execute some kind of
 * a shell command and return its output
 */
interface ShellProcess {
	String output()
}
